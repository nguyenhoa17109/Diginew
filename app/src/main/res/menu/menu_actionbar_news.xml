<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".old.NewsActivity">
    <item android:title="Menu"
        app:showAsAction="always"
        android:icon="@drawable/ic_menu">
        <menu>
            <item android:id="@+id/aRestore"
                android:title="Lưu tin"
                android:icon="@drawable/ic_restore"/>
            <item android:id="@+id/aDownload"
                android:title="Tải tin"
                app:showAsAction="never"
                android:icon="@drawable/ic_download"/>
            <item android:id="@+id/aFormatSize"
                android:title="Chỉnh sửa cỡ chữ"
                android:icon="@drawable/ic_format_size"/>
        </menu>

    </item>

</menu>